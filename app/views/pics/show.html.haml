-#13--- going to find that instac eof the pic. can use @pic because i defind the instance variable in -#'find' private meth in the controller
%h1= @pic.title
%p= @pic.description

= link_to "Back", root_path